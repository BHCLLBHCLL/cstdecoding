# CST Decoding 开发规划

> 定位：**逆向工程 + 查看器 + 编辑器**（非求解器）
> 对标：CST Studio Suite 2024 的建模 / 显示 / 项目 / 后处理查看能力
> 目标：除求解器 / 仿真运行外，**完整度 100%、深度 100%**
> 依据：`function_gap_analysis.md`（2026-08-22）+ 当前源码

---

## 0. 产品定位

本项目不再是「只读查看器」，而是：

| 角色 | 含义 | 必须达到 |
|---|---|---|
| **逆向工程** | 解析并写回 CST 私有格式（DE-ZIP、SAB、Model.mod、Parameters.json、结果文件） | 打开任意样本 .cst 无损往返；格式文档与测试覆盖所有已知结构 |
| **查看器** | 项目树、3D 几何、材料、端口、监视器、参数、结果曲线/场图 | 与 CST 同项目对照，结构与显示一致 |
| **编辑器** | 新建 / 修改 / 删除几何与工程对象，写入历史，Save / Save As | 编辑后 CST Studio 仍能打开该文件（求解设置可原样保留，本工具不跑求解） |

**明确不做（求解器 / 仿真）：**

- 时域 / 频域 / 本征模 / 积分方程 / 多层等任一求解内核
- Start / Pause / Abort 仿真、自适应网格生成、求解器 Setup 的数值求解
- 参数扫描 / 优化器的**求解循环**（UI 与任务文件可编辑、可写出，但不调用求解）

网格 **查看**（读 ModelCache / 已有网格）属于查看器，在范围内。网格 **生成**（六面体/四面体划分）视为仿真前置，放在「不做」列表，仅保留属性读写。

每个里程碑结束后：**本地测试通过 → `git commit` → `git push origin main`**。

---

## 1. 范围与验收总则

### 1.1 完整度 100%

对标 CST Ribbon / 导航树中、且不属于求解内核的每一项：

- 有对应 UI（不是永久 `_nyi` 桩）
- 有可执行行为或明确的「只读写工程文件、不求解」提示
- 有回归测试或对照用例

### 1.2 深度 100%

已实现功能须同时满足：

- 与 CST 打开同一 .cst 时，对象集合、名称、主要数值一致
- 写回后 CST Studio 能打开，历史列表可识别
- 错误可提示，不静默损坏文件
- 3D / 曲线 / 属性面板可交互，而非截图级静态展示

### 1.3 样本与对照

| 样本 | 用途 |
|---|---|
| `phone.cst` / CST Phone 5G | 导入 SAB、复杂组件树、PCB / 屏蔽罩 |
| IFA / dipole / microstrip 系列 | 纯参数化 Brick、端口、监视器、布尔 |
| RCS Ship / SAR | ModelCache、多 SAB |

不逆向 CST 可执行文件或 DLL；只做**文件格式与 UI 行为**对照。

---

## 2. 模块目标（相对差距分析）

| # | 模块 | 现状 完整/深度 | 目标 | 求解器？ |
|---|---|---:|---|---|
| 1 | 容器逆向（DE-ZIP / SAB / .mod） | 90/85 | **100/100** 含写回 | 否 |
| 2 | 文件 / 项目管理 | 25/35 | **100/100** New/Open/Save/Import/Export | 否 |
| 3 | 建模 / 几何 | 8/10 | **100/100** 基本体、布尔、变换、历史 | 否 |
| 4 | 材料 | 20/25 | **100/100** 新建/编辑/库/赋材 | 否 |
| 5 | 组件 / 分组 | 25/30 | **100/100** 增删改、拖拽、可见性 | 否 |
| 6 | 求解器 / 仿真 | 3/0 | **保持不做**（设置只读或原样写回） | **是** |
| 7 | 网格 | 3/0 | 查看 100%；生成为不做 | 生成=是 |
| 8 | 激励 / 源 | 15/20 | **100/100** 端口/场源/平面波编辑 | 否 |
| 9 | 监视器 / 探针 | 15/20 | **100/100** 创建/编辑 | 否 |
| 10 | 参数 | 18/25 | **100/100** 编辑回写；扫描/优化仅写任务 | 运行=是 |
| 11 | 后处理 / 结果 | 8/5 | **100/100** 1D/2D/3D/远场**查看与导出** | 计算=是 |
| 12 | 视图 / 显示 | 50/45 | **100/100** 剖切、标注、截图、多视口 | 否 |
| 13 | 导航树 | 85/75 | **100/100** 拖拽、双击高亮、就地改名 | 否 |
| 14 | 3D 视口 | 55/50 | **100/100** 拾取、测量、剖切、抗锯齿 | 否 |
| 15 | GUI / Ribbon | 70/35 | **100/100** 非求解按钮全部接通 | 求解按钮保持禁用 |
| 16 | 状态栏 | 60/55 | **100/100** 拾取坐标、选择集 | 求解进度 N/A |
| 17 | 宏 / 脚本 | 3/0 | **100/100** 历史 VBA 查看/编辑/追加 | 否 |

---

## 3. 优先顺序（关键节点）

顺序按「先闭环、再建模、再显示深度、再结果」：没有 Save，编辑无法验收。

```
M0  规划与基线入库
M1  项目 I/O 闭环（New / Save / Save As / 脏标记）     ← 当前开发
M2  可变工程模型 + 撤销/重做 + 参数编辑回写
M3  导航树编辑（重命名/删除/可见性/拖拽分组）+ 3D 拾取高亮
M4  基本体建模（Brick/Cylinder/Sphere/Torus/Cone）+ 历史追加
M5  布尔与变换 + 材料创建/赋材
M6  端口 / 监视器 / 探针 / 激励信号 编辑
M7  SAB 曲面深度（NURBS/交线、屏蔽罩类实体）+ 剖切/测量
M8  结果查看（.r1d 曲线、.r0d/.dat 场、远场）+ 导出
M9  视图完整（截图、包围盒、多视口、WCS 交互）
M10 宏/历史列表编辑器 + 导入导出 STEP/IGES/SAT（写 SAT 优先）
M11 网格查看（ModelCache）+ Ribbon 求解区永久禁用说明
M12 对照验收：样本矩阵 100% 查看/编辑往返
```

每个 Mx：**测试绿 → commit → push origin main**。提交信息用 `feat:` / `fix:` / `docs:`，说明「为什么」。

---

## 4. 节点详细设计

### M0 — 规划与基线

- 本文档、`.gitignore`、README 定位为逆向+查看器+编辑器
- 将可发布源码与测试纳入版本库（不含 `extracted*`、`phone.cst`、探针脚本缓存）

**完成标准：** `origin/main` 上可克隆并 `pytest tests -q`（无本地大文件依赖的用例通过）。

### M1 — 项目 I/O 闭环

- `cst_parser.write_cst`：按 DE-ZIP 写出（DE 本地头/中央目录 + 标准 EOCD）
- `open_cst`：一次读入全部条目到内存
- GUI：New（最小 Model.mod + Parameters.json）、Save、Save As、脏标记 `*`
- 往返测试：合成容器 CRC 正确；再被 `parse_central_directory` 读回

**完成标准：** 另存的 .cst 本工具能打开；条目名与内容一致。

### M2 — 工程模型与参数

- `CstProject`：entries + 解析出的 materials/components/parameters
- 参数表可编辑 expr/value/descr，写回 `Parameters.json` 与 `.mod` 中 `MakeSureParameterExists`
- Undo 栈（至少参数与对象增删）

**完成标准：** 改参数保存后重新打开数值一致。

### M3 — 树与拾取

- 导航树：Rename / Delete / Hide 写回工程；拖拽进 Group
- VTK 单击拾取实体，树与视口互相同步高亮
- 属性面板可改名称/材料（只读字段保持只读）

**完成标准：** 隐藏/改名保存后 CST 树中可见对应变化（或本工具重开一致）。

### M4 — 基本体

- Brick / Cylinder / Sphere / Torus / Cone 对话框（名称、组件、材料、范围表达式）
- 追加 Model.mod / ModelHistory.json 历史块
- 视口立即出现包围盒或解析后的网格（纯参数项目可先 AABB，再逐步精确网格）

**完成标准：** 新建 Brick 保存，本工具与 CST 均能看到该 solid。

### M5 — 布尔、变换、材料

- Add / Subtract / Intersect；Translate / Rotate / Mirror / Scale
- 材料：Normal 型 ε/μ/κ/TanD/Colour 编辑、赋给实体
- 组件 New / 删除空组件

**完成标准：** dipole/patch 类操作可在本工具完成并保存。

### M6 — 激励与监视器

- DiscretePort 创建（P1/P2、阻抗）
- Monitor（E/H/Farfield）与 Probe 创建
- 导航树 Ports / Monitors / Probes 与属性面板可编辑

**完成标准：** 与 microstrip v3 / dipole v2 对照，端口几何与阻抗正确。

### M7 — SAB 与视口深度

- intcurve / nubs 采样；屏蔽罩等复合体闭合环剖分
- 剖切平面、测量距离、选择集包围盒
- 拾取坐标进状态栏

**完成标准：** Phone PCBs / sh_cans 顶视与 CST 轮廓一致，无跨面填充。

### M8 — 结果查看

- 解析 `.r1d` 画 1D 曲线（S 参数等）
- `.r0d` / farfield `.dat` 2D/3D 伪彩（有文件则显示，无结果则空态）
- 导出 CSV / PNG

**完成标准：** 有 Result 的样本能画出至少一条 1D 曲线。

### M9 — 视图与 GUI 完成度

- Copy View / 截图、Bounding Box 模式、多视口（可选 2×2）
- Wireframe / Shading / Transparent 已有则补齐剖切与边显示开关
- 非求解 Ribbon 无 `_nyi`；求解组按钮禁用并注明「本产品不含求解器」

### M10 — 宏与 CAD 交换

- History List：查看/编辑/删除/插入 VBA 块
- 导出 SAT（ASCII）优先，STEP/IGES 视库情况（可选用现有开源写盘，禁止依赖 CST DLL）

### M11 — 网格查看

- 读取 ModelCache 中已有网格/索引并显示
- 不实现划分器；Mesh Properties 仅展示/写回已有关键字

### M12 — 验收矩阵

- 对 11 个样本：打开、另存、关键树节点计数、截图对照清单
- 差距分析表回写为 100/100（模块 6 求解器除外，标注 N/A）

---

## 5. 技术约束

- 语言：Python 3，GUI 为 PyQt5 + VTK（VTK 失败则 2D 回退）
- 写文件只通过本仓库解析器，不调用 CST 安装目录
- 不提交客户工程大文件、`extracted/`、`__pycache__`
- 测试默认 `QT_QPA_PLATFORM=offscreen`

---

## 6. 当前节点

**正在执行：M1 项目 I/O 闭环。**  
M0 随本文件与基线源码入库完成。
