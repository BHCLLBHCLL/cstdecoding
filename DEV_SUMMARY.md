# sh_cans:top 显示问题排查与修复总结

本文档总结 `cst_gui.py` 中 `sh_cans:top`（屏蔽罩顶层）渲染异常的完整排查过程、
根因分析与修复方案。问题最终表现为底边框（z=-4.275 的环形框架）上 3 处三角
面片封闭异常（细缝/孔洞），根因是**无约束 Delaunay 三角化在凹外环 + 孔洞 + 窄带
几何下丢失边框细条**。修复方案是用**孔洞桥接 + 耳切（ear-clipping）**替换带孔面的
三角化路径。修复后网格水密（0 条边界边），罐腔/罐间空隙/凹口保持开放，与 CST
Studio 显示一致。

---

## 1. 问题现象与用户反馈演进

| 轮次 | 现象 | 用户反馈 |
|------|------|----------|
| 1 | 多余线框 + 实体显示异常（小灰块） | 对比 CST 正常 |
| 2 | 线框消失但部件仍不正常 | — |
| 3 | 显示"错乱"（条纹/破洞、块状） | CST 正常 |
| 4 | 接近正确，部分面绕向仍有问题 | 几何应是**有一定厚度的实体屏蔽罩** |
| 5 | 仅剩红圈 3 处三角面片封闭异常 | — |
| 6 | （补充）CST 正确效果：**边缘是有宽度、有中断的边框** | 提供第二视角正确图 |

---

## 2. 技术管线背景

`sh_cans:top` 的网格来自 ACIS SAB（Spatial Analytical Binary）二进制实体：

```
SAB bytes
  → _scan_entities        解析实体记录（body/lump/shell/face/loop/coedge/edge/vertex/...）
  → _bodies_from_entities 按 body 分组
  → _body_mesh            逐 face 调 _tessellate_face，再 _orient_tris_outward + 焊接顶点
  → mesh {points, faces}  供 VTK 渲染（BackfaceCullingOn + Flat 着色）
```

关键概念：

- **face / loop / coedge**：一个 face 由若干 loop（外环 + 孔环）围成，loop 由 coedge
  串成。`_walk_loop` 沿指针恢复闭合多边形。
- **三角化**：平面面由 `_triangulate_plane` 把"外环 + 孔环"三角化。
- **背面剔除**：GUI 开启 `BackfaceCullingOn`，三角形绕序不一致会被剔除，形成"破洞"。
- **薄壁实体屏蔽罩**：顶盖（z=-3.2775 整板）+ 侧墙 + 底边框（z=-4.275 环形框架）
  围成封闭实体，罐腔是内部空腔（底层开放、顶层被盖）。

---

## 3. 各阶段根因与修复

### 3.1 多余线框
ACIS 边采样把局部坐标系的控制点（nubs 网）当成世界坐标点绘出。
**修复**：CAD 线框采样剔除越出实体 AABB 的局部坐标点。

### 3.2 异常实体（跨罐灰色大板）
多罐轮廓被变体 coedge 布局焊成一张跨罐 spanning 面，盖住罐间 PCB 空隙。
**修复**：`_drop_weld_edges` / `_cluster_edge_groups` 按几何簇分离多罐轮廓，
保留凹口（不取凸包）。

### 3.3 实体缺墙/形状错误
SAB 指针错位导致面解析错误。**修复**：对齐实体指针解析。

### 3.4 显示"错乱"（条纹/破洞）
**根因**：各面绕序不一致 —— 逐面 tessellation 只对齐 ACIS *surface* 法线、忽略
face 的 *sense* 标志，且 hull/plane-cluster 等回退路径完全绕过了对齐，导致单个面
（尤其底边框）绕序内外混杂。背面剔除把内朝向三角形剔除，形成条纹状破洞。

**修复**：`_orient_tris_outward`（sab_bodies.py:2154）—— 对装配后的全部三角形
做**逐三角形、双侧竖直射线实体测试**，把法线翻成实体外侧，保证闭合实体绕序一致。

### 3.5 残留绕向问题（z-fighting / 共面重叠）
**根因**：底面 face4（ent27202，一条 28 mm² 小底边）的指针走查其实成功恢复了小
闭合多边形，但因面积远小于整底面（~700 mm²）被误判为"走查几乎没恢复"，错误触发
`wrap_face` 第 4 条回退，把**整个底面**（含罐腔 A）三角化，与 face0 共面重叠 →
110 对共面重叠三角形 → z-fight 条纹；另有 39 条非流形边、5 条边界边。

**修复**：新增 `walk_recovered` 守卫（sab_bodies.py:1996）——只要走查闭合了任一
真实环即视为恢复成功，回退分支（:2049）附加 `and not walk_recovered`，避免对合法
小面误触发。结果：共面重叠 110→0，非流形边 39→1。

**附带修正**：该修复暴露 `test_sh_cans_top_not_spanning` 旧断言错误 —— ACIS 模型
中罐腔 A 底层本就开放（face0 把罐 A 当孔洞，并不存在罐 A 底板面），此前断言"罐 A
被覆盖"是靠 face4 的 bug 误填才通过。已把断言改为 `not _mesh_covers(...)` 并更新注释。

### 3.6 红圈 3 处三角面片封闭异常（本次会话重点）

**现象**：底边框在 3 处出现细缝/孔洞（边界边诊断：9 条边界边、3 个簇）：

| 缺口 | 位置 | 尺寸 | 交界 |
|------|------|------|------|
| 0 | x≈33–34, y −20.6~−28.7 | ~1mm × 8mm 竖缝 | 底面 + 侧墙(−x) |
| 1 | x≈54–55, y −8.8~−17.9 | ~1mm × 9mm 竖缝 | 底面 + 侧墙(+x) |
| 2 | x≈52.6–76.7, y −28.7~−26.9 | ~24mm × 1.8mm 长缝 | 底面 + 侧墙(−y) |

**定位**：3 处缺口都属于主底边框面 **face[27171]**（法线 −z，外环 24v/507mm² +
孔环 11v/376mm²，环间即 1mm 宽边框带）。`_tessellate_face` 恢复的环完全正确，
问题出在 `_triangulate_plane` 的**带孔三角化路径**。

**根因**：带孔面走 `_delaunay_in_region`（无约束 Delaunay + 质心/边中点区域测试）。
外环是**凹的**（在台阶处内外环凹凸错位 ~1mm），无约束 Delaunay 在窄带处生成的是
**横跨凹口/孔洞的大三角形**，其质心落到外环外或孔内，被 `_tri_in_face_region`
**正确地**拒绝；而窄带本身又没有合法小三角形去填 → 3 段边框被丢成洞。

逐步诊断证据（`_step_debug`）：被丢三角形质心 `in_outer=False`，确认是"跨凹口
大三角形被拒、窄带无人填"，而非区域测试误杀合法三角形。

**修复**：把带孔面的三角化从"无约束 Delaunay"改为**孔洞桥接 + 耳切**：

1. `_bridge_holes_into_outer`（sab_bodies.py:1339）：把每个孔环通过一条零宽"桥"
   并入外环，得到单一简单多边形 —— 桥两端点各复制一次形成通道，使所有真实面边
   都成为三角形边，耳切自然填满边框窄带、保持孔洞开放。孔按"最右优先"排序，避免
   桥跨越尚未并入的孔。
2. `_bridge_vertex`（:1293）：桥接顶点选"离孔顶点最近、且桥接线不与任何外环边/
   孔边相交（可见）"的外环顶点。**关键**：初版用"射线 + 三角形内点精炼"，被投影
   旋转 + 凹口误导，桥接到 12mm 外远顶点并 crossing 外环边 → 自交 → 耳切面积只剩
   一半；改为显式 `_seg_proper_cross`（:1284）可见性检查后桥接干净。
3. `_triangulate_plane`（:1401）带孔分支：桥接 + 耳切为主，**面积校验 + 孔开放
   校验**通过才采用，否则回退 Delaunay（旧行为），保证严格不劣化。

**校验的两个坑**：

- `_poly_area2` 返回鞋带和（**2×面积**），`_tris_area2` 返回真实面积（÷2）。
  初版 `want = outer − hole` 用了 2× 值，导致 `got/want = 0.5` 被判失败而误回退
  —— 实际桥接结果经网格地面真值验证 **100% 覆盖真实边框（10058/10058，0 漏）**。
  已把参考面积统一为真实面积（×0.5）。
- 新增 `_tris_keep_holes_open`（:1383）：若某孔桥接失败被跳过，耳切会把它填上，
  仅靠面积比仍会误判通过；该校验直接拒绝"三角形质心落入孔内"的结果，保证孔洞
  不被误填。

---

## 4. 验证结果

- **网格水密**：边界边 9 → **0**（3 处缺口全部封闭，且无其它伪边界边）。
- **无过度填充**：罐腔 A、罐间空隙 (60,−15)/(62,−20)、左下凹口 (32,−23) 在底层
  保持开放；顶盖整板连续。
- **视觉**：GUI 顶视图两个罐腔开口干净，3D 视图呈带厚度实体屏蔽罩（围框 + 墙体 +
  顶盖），与 CST Studio 两视角参考一致，3 处异常消失。
- **测试**：`tests/test_sab_bodies.py` 16 项 + 全套 `tests/` **82 项全部通过**，
  无回归。

---

## 5. 关键代码位置（sab_bodies.py）

| 函数 | 行号 | 作用 |
|------|------|------|
| `_triangulate_plane` | 1401 | 平面三角化入口；带孔面改走桥接+耳切 |
| `_bridge_holes_into_outer` | 1339 | 孔洞桥接成单一简单多边形 |
| `_bridge_vertex` | 1293 | 选可见桥接顶点（最近 + 不 crossing） |
| `_seg_proper_cross` | 1284 | 线段严格相交判定（可见性基础） |
| `_tris_area2` / `_tris_keep_holes_open` | 1375 / 1383 | 面积校验 + 孔开放校验 |
| `_delaunay_in_region` | 1207 | 旧无约束 Delaunay 路径（现作回退） |
| `_earclip` | 959 | 耳切（简单多边形） |
| `_tessellate_face` | 1844 | 单面 tessellation / 各回退分支 |
| `_orient_tris_outward` | 2154 | 逐三角形外翻成实体外侧（治条纹） |
| `walk_recovered` 守卫 | 1996 / 2049 | 防小面误触发 wrap 回退（治 z-fight） |

---

## 6. 设计要点 / 经验教训

1. **无约束 Delaunay + 质心采样 ≠ 约束三角化**。对"凹外环 + 孔洞 + 窄带"几何，
   它要么生成跨凹口的三角形（被区域测试正确拒绝），要么漏掉窄带。带孔面必须用
   约束（孔洞桥接 / CDT）保证边界边成为三角形边。
2. **桥接的零宽通道是耳切的薄弱环节**：桥接线必须严格不 crossing 任何边（用显式
   可见性检查，而非仅查三角形内点），否则合并多边形自交、耳切面积塌半。
3. **校验口径要统一**：鞋带面积（2×）与真实面积混用会让覆盖率校验误判，导致正确
   结果被误回退。
4. **回退要安全**：新路径（桥接+耳切）必须通过"面积达标 + 孔不填"双重校验才采用，
   否则回退到旧路径，保证改动严格不劣化。
5. **网格地面真值 > 面积启发式**：对边界/覆盖类问题，用网格点采样做"真实区域 vs
   实际覆盖"对比，比单看面积数字更可靠。
